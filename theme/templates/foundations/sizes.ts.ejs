// AUTO-GENERATED - File generated by `figma2theme`, manual edits may be overwritten

export const baseSizes = <%- JSON.stringify(chakra.spacing) %>;

export type BaseSizes = typeof baseSizes;

const largeSizes = <%- JSON.stringify(chakra.sizes) %>;

const container = {
  sm: '640px',
  md: '768px',
  lg: '1024px',
  xl: '1280px',
};

const sizes = {
  ...baseSizes,
  ...largeSizes,
  container,
};

export type Sizes = typeof baseSizes &
  typeof largeSizes & { container: typeof container };

export default sizes;
