// AUTO-GENERATED - File generated by figma2theme <%- version %>, manual edits may be overwritten
  // Figma file: https://www.figma.com/file/<%- figmaFileKey %>
    // File version selected: <%- versionDescription %>
      // Generated at: <%- new Date().toString() %>

        const plugin = require('tailwindcss/plugin');
        const withLocalChanges = require('./tailwind.config.local');

        module.exports = withLocalChanges({
        // Content is the paths to all the HTML templates, JS components and
        // any other files that contain Tailwind class names.
        // An array of path globs, eg './components/**/*.{html,js}'
        content: <%- JSON.stringify(tailwind.content) %>,
          theme: {
          // You define your project’s breakpoints in the theme.screens section
          // of your tailwind.config.js file. The keys become your responsive
          // modifiers (like md:text-center), and the values are the min-width
          // where that breakpoint should start.
          screens: <%- JSON.stringify(tailwind.theme.screens) %>,
            // These colors will be made available everywhere in the framework where you use colors,
            // like the text color utilities, border color utilities, background color utilities, and more.
            colors: <%- JSON.stringify(tailwind.theme.colors) %>,
              // The spacing scale is inherited by the padding, margin, width, height, maxHeight, gap, inset, space, and
              // translate core plugins.
              spacing: <%- JSON.stringify(tailwind.theme.spacing) %>,
                // Use the drop-shadow-{amount} utilities to add a drop shadow to an element.
                dropShadow: <%- JSON.stringify(tailwind.theme.dropShadow) %>,

                  // Use these as font-NAME for setting font family.
                  fontFamily: <%- JSON.stringify(tailwind.theme.fontFamily) %>,
                    // Set text sizes with text-NAME
                    fontSize: <%- JSON.stringify(tailwind.theme.fontSize) %>,
                      // Line height is leading-NAME
                      lineHeight: <%- JSON.stringify(tailwind.theme.lineHeight) %>,
                        // Letter spacing is tracking-NAME
                        letterSpacing: <%- JSON.stringify(tailwind.theme.letterSpacing) %>,
                          },
                          plugins: [
                          plugin(({addComponents}) => {
                          addComponents(<%- JSON.stringify(tailwind.componentDefinitions) %>)
                            })]
                            });
