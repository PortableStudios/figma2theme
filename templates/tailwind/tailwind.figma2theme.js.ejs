/**
 * AUTO-GENERATED! This file was generated by Portable's `figma2theme` tool (v<%- version %>).
 * If changes were made to the UI Kit in Figma then you should run `figma2theme` again to replace this file rather than editing it manually.
 *
 * This file exports a Tailwind "preset" which can be imported into `tailwind.config.js` and used as the base configuration.
 * The configuration is based on the design tokens in your UI Kit file. For more information see the following resources:
 * - `figma2theme` README: https://github.com/PortableStudios/figma2theme
 * - Presets documentation: https://tailwindcss.com/docs/presets
 *
 * Generation metadata:
 * - UI Kit file used: <%- tailwind.urls.file %>
 * - File version selected: <%- versionDescription %>
 * - Generated at: <%- new Date().toString() %>
 */

import plugin from 'tailwindcss/plugin';

/** @type {import('tailwindcss').Config} */
const config = {
  theme: {
    extend: {
      // Breakpoints from the UI Kit (<%- tailwind.urls.breakpoints %>)
      screens: <%- JSON.stringify(tailwind.screens, undefined, 2) %>,
      // Colours from the UI Kit (<%- tailwind.urls.colours %>)
      colors: <%- JSON.stringify(tailwind.colors, undefined, 2) %>,
      // Radii from the UI Kit (<%- tailwind.urls.radii %>)
      borderRadius: <%- JSON.stringify(tailwind.borderRadius, undefined, 2) %>,
      // Shadows from the UI Kit (<%- tailwind.urls.shadows %>)
      boxShadow: <%- JSON.stringify(tailwind.boxShadow, undefined, 2) %>,
      // Font families from the UI Kit (<%- tailwind.urls.typography %>)
      fontFamily: <%- JSON.stringify(tailwind.fontFamily, undefined, 2) %>,
      // Font sizes from the UI Kit (<%- tailwind.urls.typography %>)
      fontSize: <%- JSON.stringify(tailwind.fontSize, undefined, 2) %>,
      // Line heights from the UI Kit (<%- tailwind.urls.typography %>)
      lineHeight: <%- JSON.stringify(tailwind.lineHeight, undefined, 2) %>,
      // Letter spacings from the UI Kit (<%- tailwind.urls.typography %>)
      letterSpacing: <%- JSON.stringify(tailwind.letterSpacing, undefined, 2) %>,
    },
  },
  plugins: [
    // Text styles from the UI Kit (<%- tailwind.urls.typography %>)
    plugin(function textStylesPlugin({ addComponents }) {
      addComponents(<%- JSON.stringify(tailwind.textStyles, undefined, 2) %>);
    }),
  ],
};

export default config;
